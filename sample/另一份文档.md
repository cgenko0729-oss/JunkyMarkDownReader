# 另一份文档

这份文档存在的意义是验证「本地 `.md` 链接能在应用内跳转」这件事。

如果你是从示例文档里点链接过来的，说明跳转正常工作了。

## 返回

- [回到示例文档](./示例文档.md)

## 顺便测一下

| 项 | 值 |
|---|---|
| 跳转方式 | 主进程渲染时给本地链接打上 `data-local-path` |
| 谁处理点击 | 渲染进程的事件委托，拦下默认导航后调 `openDocument()` |
| 非 md 的本地文件 | 不在应用内打开，改为在资源管理器里定位 |
